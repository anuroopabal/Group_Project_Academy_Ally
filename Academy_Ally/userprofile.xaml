 
    namespace YourAppName
{
    public partial class UserProfilePage : ContentPage
    {
        public UserProfilePage()
        {
            InitializeComponent();
            LoadUserProfile();
        }

        public object EmailLabel { get; private set; }
        public object NameLabel { get; private set; }
        public object PhoneNumberLabel { get; private set; }
        public object AddressLabel { get; private set; }
        public object CourseNameLabel { get; private set; }

        private void InitializeComponent()
        {
            throw new NotImplementedException();
        }

        private void LoadUserProfile()
        {
            // Load user profile details
            EmailLabel = "john@graph.com";
            NameLabel = "Jon Doe";
            PhoneNumberLabel = "+1 xxx-xxx-xxxx";
            AddressLabel = "123, ABC, Kitchener, ON";
            CourseNameLabel= "[Course Name]";
            // Password is typically not displayed in a user profile page for security reasons
        }
    }
}

